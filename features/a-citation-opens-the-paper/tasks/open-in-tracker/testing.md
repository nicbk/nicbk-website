# Testing: Open in Tracker

The feature's unit rows for the action and its absence, and the browser rows for
what it opens and the path it leaves.

Mutation checks: rendering the action for an edge with no article; dropping
`via` from the link.
