# Testing: The Entry is Located

The feature's unit and integration rows for the parser and the writer, plus the
browser row that checks a stored rectangle against the previewed region.

**That check is the task's output, not a formality.** Report the page and both
rectangles as numbers. Everything after this task assumes they agree.
