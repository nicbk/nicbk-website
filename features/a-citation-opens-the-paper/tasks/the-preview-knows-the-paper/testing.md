# Testing: The Preview Knows the Paper

The feature's unit rows for the matching rule, against rectangles taken from a
real bibliography rather than invented ones.

Mutation checks: matching on overlap without the page; returning the first of
two overlapping entries instead of none.
