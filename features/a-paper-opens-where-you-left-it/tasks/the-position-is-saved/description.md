# Task: The Position Is Saved

Task 1 of 2 of [#23](../../description.md). Sub-issue
[#195](https://github.com/nicbk/nicbk-website/issues/195).

`reading_page` and `reading_offset` on `articles`, their migration and Zero
schema entries, and `articles.setReadingPosition` — shaped exactly like
`articles.setStatus`. No UI; task 2 is the only caller.
