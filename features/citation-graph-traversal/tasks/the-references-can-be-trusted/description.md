# Task: The References Can Be Trusted

Task 1 of 4 of [#10](../../description.md). Sub-issue
[#202](https://github.com/nicbk/nicbk-website/issues/202).

Keep what the paper printed for each reference (`citation_edges.raw_text`),
record how many references Semantic Scholar says it has
(`articles.reference_count`), drop rows GROBID merged, and backfill existing
papers. No UI.
